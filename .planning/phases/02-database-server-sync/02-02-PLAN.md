---
wave: 2
depends_on: ["02-01"]
files_modified: ["apps/api/src/sync/sync.module.ts", "apps/api/src/sync/sync.service.ts", "apps/api/package.json"]
autonomous: true
requirements: ["BACK-03", "BACK-04", "BACK-05"]
---

# Plan 02-02: Markdown Ingestion and Sync Script

## Objective
Develop the automated synchronization mechanism (`sync.service.ts`) capable of reading local static `.md` files, parsing their filenames, and safely upserting them into the Postgres database.

## Tasks

<task>
<read_first>
- apps/api/prisma/schema.prisma
</read_first>
<action>
Install file system traversal utilities if needed. Create `apps/api/src/sync/sync.module.ts` and `apps/api/src/sync/sync.service.ts`. The `SyncService` must establish a connection or use a `PrismaService` to talk to the DB. Create an entrypoint (such as a NestJS Command package or a vanilla NPM script using `ts-node`) in `apps/api/package.json` to execute this sync script via `pnpm run sync`.
</action>
<acceptance_criteria>
- `apps/api/src/sync/sync.service.ts` contains `class SyncService`
- `apps/api/package.json` scripts has `"sync":`
</acceptance_criteria>
</task>

<task>
<read_first>
- apps/api/src/sync/sync.service.ts
</read_first>
<action>
Implement the core file iteration logic in `SyncService`. Iterate through `../../homilies` (relative to `apps/api` directory). Use a `try/catch` loop block so that failures on a single file log a custom `Logger.warn` and do not crash the entire process.
Extract metadata from filenames utilizing a regex like `^(.*?)\s*-\s*(.*?)\s*-\s*(.*?)(?:\s*-\s*(.*?))?\.md$`.
Read the raw `.md` file content.
</action>
<acceptance_criteria>
- `sync.service.ts` contains `Logger.warn` or `console.warn` inside the loop `catch` block
- `sync.service.ts` contains `fs.promises.readdir` or equivalent glob usage pointing to the homilies directory
</acceptance_criteria>
</task>

<task>
<read_first>
- apps/api/src/sync/sync.service.ts
</read_first>
<action>
Implement the database UPSERT per Homily. Inside the loop, map the extracted metadata into the `Prisma.HomilyCreateInput` shape. Use `prisma.homily.upsert({ where: { originalFile: filename }, update: { ... }, create: { ... } })` to persist the record. Generate the `slug` dynamically by slugifying the filename/title.
</action>
<acceptance_criteria>
- `sync.service.ts` contains `upsert(`
- `sync.service.ts` contains `where: { originalFile:`
</acceptance_criteria>
</task>

## Verification
- Test file parser with a dummy filename regex string effectively outputting metadata.

## Must Haves
- Script can be invoked gracefully.
- Parsing failures only log a warning and continue.
- Re-running the script results in idempotent updates, not duplicating SQL rows.
