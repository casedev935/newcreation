---
wave: 2
depends_on: ["01-01"]
files_modified:
  - ".github/workflows/deploy.yml"
  - "docker-compose.yml"
  - "docker-compose-override.yml"
  - "apps/api/Dockerfile"
  - "apps/web/Dockerfile"
  - ".env.example"
  - "apps/api/.dockerignore"
  - "apps/web/.dockerignore"
autonomous: true
requirements_addressed:
  - INFR-02
  - INFR-03
  - INFR-04
  - INFR-05
---

## Objective
Establish the VPS CI/CD deployment pipeline via GitHub Actions and Docker orchestration including network mapping for Traefik proxy.

## Tasks

<task>
<id>01-02-1</id>
<action>
Create initial standalone `apps/web/Dockerfile` using `node:20-slim`. Include explicit handling for Next.js build cache and pnpm lockfile.
</action>
<read_first>
- `.planning/phases/01-foundation-deployability/01-CONTEXT.md`
</read_first>
<acceptance_criteria>
`apps/web/Dockerfile` exists with `FROM node:20-slim` and sets `ENV PORT 3000`.
</acceptance_criteria>
</task>

<task>
<id>01-02-2</id>
<action>
Create `apps/api/Dockerfile` using `node:20-slim`. Define production behavior for NestJS. Create `.dockerignore` files for both apps omitting `node_modules` and heavy caches.
</action>
<read_first>
- `apps/web/Dockerfile`
</read_first>
<acceptance_criteria>
`apps/api/Dockerfile` exists and runs `node dist/main`. `.dockerignore` exists in both dirs.
</acceptance_criteria>
</task>

<task>
<id>01-02-3</id>
<action>
Create `./docker-compose.yml` defining `web` and `api` services. Include an external network `gateway_network` to bridge with the existing Traefik. Include logging limits (`max-size: "10m"`).
</action>
<read_first>
- `.planning/phases/01-foundation-deployability/01-CONTEXT.md`
</read_first>
<acceptance_criteria>
`docker-compose.yml` contains `networks:\n  gateway_network:\n    external: true`. Traefik routing labels exist mapping to `newcreation.mediamanager.tech`.
</acceptance_criteria>
</task>

<task>
<id>01-02-4</id>
<action>
Create `./docker-compose-override.yml` exposing local ports and mounting volume bindings for `pnpm dev` hot-reloading. Create `.env.example` placeholder text.
</action>
<read_first>
- `docker-compose.yml`
</read_first>
<acceptance_criteria>
`docker-compose-override.yml` specifies exposed localhost ports `3000:3000` and `3001:3001` and volume syncs.
</acceptance_criteria>
</task>

<task>
<id>01-02-5</id>
<action>
Create `.github/workflows/deploy.yml` with steps invoking `docker compose build/up` via standard action runner SSH remote to VPS server for continuous integration.
</action>
<read_first>
- `.planning/phases/01-foundation-deployability/01-CONTEXT.md`
</read_first>
<acceptance_criteria>
`deploy.yml` defines pushing steps or triggers on branch `main`/`master` deploying via SSH.
</acceptance_criteria>
</task>

## Verification
- Local Docker builds pass (`docker compose build` without errors).
- Compose syntax is valid (`docker compose config`).

## Must Haves
- Logging is correctly rate-limited in services to prevent SSD exhaustion.
- Base images are strictly `node:20-slim`.
- Remote proxy auto-discover bounds are fulfilled with `gateway_network`.
