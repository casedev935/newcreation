---
wave: 1
depends_on: []
files_modified:
  - "package.json"
  - "turbo.json"
  - "pnpm-workspace.yaml"
  - ".geminignore"
  - ".gitignore"
autonomous: true
requirements_addressed:
  - INFR-01
---

## Objective
Initialize the root Turborepo workspaces using `pnpm` and configure essential project management files like ignore settings and code formatters.

## Tasks

<task>
<id>01-01-1</id>
<action>
Initialize `pnpm-workspace.yaml` in the root directory to define the `apps/*` and `packages/*` workspace mapping.
</action>
<read_first>
- `.planning/phases/01-foundation-deployability/01-CONTEXT.md`
</read_first>
<acceptance_criteria>
`pnpm-workspace.yaml` exists and contains `packages:\n  - 'apps/*'\n  - 'packages/*'`
</acceptance_criteria>
</task>

<task>
<id>01-01-2</id>
<action>
Create the base `package.json` in the workspace root configuring `pnpm` as the package manager and including Turborepo dependencies.
</action>
<read_first>
- `package.json`
</read_first>
<acceptance_criteria>
`package.json` contains `"workspaces": ["apps/*", "packages/*"]` and `"packageManager": "pnpm@..."`.
</acceptance_criteria>
</task>

<task>
<id>01-01-3</id>
<action>
Create `turbo.json` config specifying pipeline defaults (build, test, dev, lint) ensuring caching is enabled for CI pipelines.
</action>
<read_first>
- `pnpm-workspace.yaml`
</read_first>
<acceptance_criteria>
`turbo.json` contains a `pipeline` key with `build`, `lint`, and `dev` scripts defined.
</acceptance_criteria>
</task>

<task>
<id>01-01-4</id>
<action>
Create `.geminignore` with paths preventing deep AI parsing over heavy folders (`node_modules/`, `dist/`, `.next/`, `postgres-data/`). Update root `.gitignore`.
</action>
<read_first>
- `.planning/phases/01-foundation-deployability/01-CONTEXT.md`
</read_first>
<acceptance_criteria>
`.geminignore` exists containing `node_modules/` and `postgres-data/`.
</acceptance_criteria>
</task>

## Verification
- Run `pnpm install` in the root folder successfully without warnings about invalid workspaces.
- Confirm `turbo --version` runs properly in the context.

## Must Haves
- The project foundation uses Turborepo and pnpm explicitly.
- The Git and LLM ignore files prevent system bloat.
